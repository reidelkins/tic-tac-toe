apiVersion: v1
kind: Service
metadata:
  name: {{ include "tic-tac-toe-frontend.fullname" . }}
  labels:
    {{- include "tic-tac-toe-frontend.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "tic-tac-toe-frontend.selectorLabels" . | nindent 4 }}
