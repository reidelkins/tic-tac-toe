apiVersion: v1
kind: Service
metadata:
  name: {{ include "tic-tac-toe-backend.fullname" . }}
  labels:
    {{- include "tic-tac-toe-backend.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "tic-tac-toe-backend.selectorLabels" . | nindent 4 }}
